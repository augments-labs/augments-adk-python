name: Security

# Supply-chain (pip-audit) + static (bandit) scans. Both are plain CLI tools,
# so they run on a private repo without GitHub Advanced Security. CodeQL —
# which DOES require GHAS on a private repo — lives in codeql.yml.

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    - cron: "0 6 * * 1" # weekly, Monday 06:00 UTC

permissions:
  contents: read

concurrency:
  group: security-${{ github.ref }}
  cancel-in-progress: true

# Export exactly what uv.lock pins; never re-resolve silently.
env:
  UV_FROZEN: "1"

jobs:
  pip-audit:
    name: Dependency CVE scan (pip-audit)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: astral-sh/setup-uv@v10.1.0
        with:
          enable-cache: true
          python-version: "3.12"
      # Audit the graph uv.lock actually pins rather than a fresh resolution,
      # so the scan and the installed environment can never disagree.
      - name: Export locked runtime dependencies
        run: uv export --no-dev --no-emit-project --no-hashes --format requirements.txt -o requirements-audit.txt
      - run: >-
          uvx --from "pip-audit>=2.7" pip-audit
          --progress-spinner=off
          -r requirements-audit.txt

  bandit:
    name: Static security scan (bandit)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: astral-sh/setup-uv@v10.1.0
        with:
          enable-cache: true
          python-version: "3.12"
      # Non-blocking until the pre-existing findings are triaged (tracked issue):
      # mostly framework subprocess / random / tmp-path patterns (sandbox/, rag/)
      # plus safe parameterized SQL. The scan still runs and reports on every
      # run; flip to blocking once the findings are reviewed/suppressed.
      # Config (exclude_dirs, skip B101) lives in [tool.bandit] in pyproject.
      - name: bandit (non-blocking, pending triage)
        continue-on-error: true
        run: uvx --from "bandit[toml]>=1.7" bandit -c pyproject.toml -r src
